// Permission protocol compatibility exports. The canonical declarations live
// in harness-permissions; tools consume their shapes but do not own policy.
export type {
  PermissionResource,
  ToolCallInfo,
  ToolSideEffect,
} from "@innocencecode/harness-permissions";
